## Description
Briefly describe the changes introduced by this pull request.

## Checklist
- [ ] **Zero Dependencies**: `package.json` has NO third-party runtime or dev dependencies (`dependencies: {}`, `devDependencies: {}`).
- [ ] **Tests Passing**: All tests pass via `npm test` (`node --test`).
- [ ] **Reproducible Build Verified**: `npm run build` and `npm run verify:build` produce byte-identical hashes.
- [ ] **Documentation**: Updated `README.md` or `STDLIB.md` if adding a new feature or standard library substitution.
- [ ] **Code Quality**: Adheres to existing standard library idiomatic practices and strict error handling.
